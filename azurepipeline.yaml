trigger: none

#pool:
 # vmImage: 'ubuntu-latest'

stages:
- stage: building_haha
  jobs:

   - job: buildi_babaha
     pool:
      vmImage: 'ubuntu-latest'
     steps:
     - script: |
              cd ~
              ls 
              touch haha
              ls    
              echo "HOSTNAME=$(hostname)"
           
   - job:  build_babaha
     pool:
      vmImage: 'widows-latest'
     dependsOn: buildi_babaha
     steps:
     - script: |
              cd ~
              ls 
              echo "HOSTNAME=$(hostname)"

