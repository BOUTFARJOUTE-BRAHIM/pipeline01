trigger: 
  - main

#pool:
 # vmImage: 'ubuntu-latest'
variables:
  app: hostnamectl
stages:
- stage: building_haha
  jobs:
   - job: buildi_babaha
     container: ubuntu:22.04

     #pool:
      #vmImage: 'ubuntu-latest'
     steps:
     - script: |
              mkdir ficher01
              echo "hello hi hello hi" > ficher01
              $(app)
              cd ~
              ls 
              touch haha
              ls    
              echo "HOSTNAME=$(hostname)"
     - publish: ficher01
       artifact: R    
   - job:  build_babaha
    # pool:
     # vmImage: 'windows-2022'
     dependsOn: buildi_babaha
     steps:
     - download: ficher01
       artifact: R

     - script: |
              cat ficher01
              cd ~
              ls 
              echo "HOSTNAME=$(hostname)"
     










