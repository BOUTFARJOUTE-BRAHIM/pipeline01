trigger: 
  - main

#pool:
 # vmImage: 'ubuntu-latest'
variables:
  app: hostnamectl
stages:
- stage: building_haha
  jobs:
   - job: buildi_babaha
     container: ubuntu:22.04

     #pool:
      #vmImage: 'ubuntu-latest'
     steps:
     - script: |
              mkdir ficher01
              echo "hello hi hell" > ficher01/message.txt
              $(app)
              cd ~
              ls 
              touch haha
              ls    
              echo "HOSTNAME=$(hostname)"
     - publish: ficher01
       artifact: R    
   - job:  build_babaha
    # pool:
     # vmImage: 'windows-2022'
     dependsOn: buildi_babaha
     steps:
     - download: current
       artifact: R

     - script: |
              cd $(Pipeline.Workspace)/R
              cat message.txt
              cd ~
              ls 
              echo "HOSTNAME=$(hostname)"
     










